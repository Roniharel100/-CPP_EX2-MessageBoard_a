#include "Board.hpp"

namespace ariel {
    void Board::post(uint row, uint column, Direction direction, const std::string &message) { }
    std::string Board::read(uint row, uint column, Direction direction, uint length) { return ""; }
    void Board::show() {
    }
}